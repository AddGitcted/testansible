 - name: "Install base"
   hosts:  web
   vars:
     app_user: bdd
   roles:
      - { role: base, tags: ["base"] }
      - { role: Nginx }

 - name: "deploy bdd Nginx"
   hosts: web
   become: true
   vars: 
    app_user: bdd
    pathroot: '/home/{{ app_user }}'
   roles:
    - { role: bdd, tags: ["michelle_obama"] }
    - { role: Nginx }
     
